apiVersion:	networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allowports811
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: my
  # ingress:
  # - ports: 
  #   - port: 81
