apiVersion: v1
kind:	Pod
metadata:
  name:	mypod
  namespace: default
spec: 
  containers:
  - image: nginx
    name: nginx-containers
    ports:
    - containerPort: 80

  - image: linuxserver/firefox
    name: firefox-containers
    ports:
    - containerPort: 3000